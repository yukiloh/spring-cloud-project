spring:
  application:
    name: spring-cloud-web-admin-feign

server:
  port: 8765

feign:
#  开启熔断机制
  hystrix:
    enabled: true

  #  允许bean被复写,暂时搁置
  main:
    allow-bean-definition-overriding: true